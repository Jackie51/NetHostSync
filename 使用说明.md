# 网络配置一键切换脚本 · 使用说明（精简版）

## 一、两条核心规则

| 适配器类型 | 行为 |
|------------|------|
| **有线网络** | 自动套用**默认静态 IP**（可手动改具体数值） |
| **无线网络** | **始终动态获取 IP（DHCP）** |

> 设计目标：日常插上网线即用固定 IP（方便局域网访问/端口映射），无线随手连、自动 DHCP，无需任何手动配置。有线配静态、无线走 DHCP，配合接口优先级实现「插线优先走有线、拔线自动切无线」。

## 二、文件清单

| 文件 | 作用 |
|------|------|
| `NetHostSync.bat` | 双击启动器（纯 ASCII，任意系统代码页都不会乱码）。日常只需双击它。 |
| `network_config.ps1` | 主逻辑脚本（PowerShell，原生支持中文）。由 `.bat` 自动调用，不要单独双击。 |
| `config.json` | **外部配置文件**（首次运行自动生成）。改网络参数不用碰 `.ps1`。 |
| `backup.json` | 切换前自动生成的备份（恢复用，脚本自动维护）。 |
| `network_switch.log` | 自动触发模式下的运行日志（排错用，自动维护）。 |

> 为什么分两个文件：`.bat` 里塞中文在 Windows 下极易因代码页不一致而乱码，所以启动器保持纯英文无中文，真正的中文界面放在 `.ps1` 里（PowerShell 原生 Unicode，不受代码页影响）。

## 三、如何运行

1. **双击 `NetHostSync.bat`** 即可。
2. 若当前不是管理员，会弹出 UAC 提权窗口 → 点「是」，脚本自动以管理员身份重启（设置 IP 必须管理员权限）。
3. 出现菜单后，输入数字回车。

## 四、菜单功能

| 选项 | 功能 | 说明 |
|------|------|------|
| `1` 自动切换 | 一键按规则配置所有网卡 | **有线→默认静态、无线→DHCP**，自动枚举所有「已连接」网卡；完成后**同步刷新 hosts**（把本地服务地址指向当前 IPv4） |
| `2` 设置有线静态 IP | 手动更改有线静态值 | 弹出提示，每个参数都有默认值，直接回车即用，也可输入新值修改；多网卡时让你选一张 |
| `3` 设置无线为 DHCP | 强制无线动态获取 | 把选中的无线网卡设回 DHCP（一般无需手动，自动模式已处理） |
| `4` 启用自动触发 | 注册「网络变化自动触发」计划任务 | 插拔网线 / 连不同 Wi-Fi 时**自动修正配置**；对已正确的连接**不干扰**，不会打断正在进行的 DHCP 握手 |
| `5` 停用自动触发 | 卸载上述计划任务 | — |
| `6` 恢复上一次配置 | 从 `backup.json` 一键回滚 | 误操作 / 想回到切换前状态时使用 |
| `7` 诊断（只读） | 打印各网卡真实状态 + 自动模式将执行什么 | **排错专用，不改任何配置**；出问题把输出原样发回即可 |
| `8` 退出 | 结束脚本 | — |

> 每次切换（选项 1/2/3）**操作前都会自动备份**当前配置到 `backup.json`；切换后还会做**连通性校验**（ping 网关与 DNS），并在末尾打印当前配置状态。

## 五、自动模式（选项 1）的判定逻辑

脚本会**逐张「已连接」网卡**判断类型并配置：

| 适配器类型 | 识别方式 | 应用配置 |
|------------|----------|----------|
| 有线（以太网） | `netsh wlan` 不将其列为无线，且非 `Wireless80211` | **静态**：默认 IP `192.168.1.100` + 优先级 10 |
| 无线（Wi-Fi） | `netsh wlan show interfaces` 确认其为无线网卡 | **DHCP 动态获取** |

- **无线判定**：以 `netsh wlan show interfaces` 为准（该命令只列出无线网卡），并精确对应到当前适配器名称，避免多网卡抓错。
- **有线优先级**：有线静态 `metric=10`、无线 DHCP 现已**显式设定 `metric=20`**，因此「有线/无线/手机热点同时连接时，系统路由确定性优先走有线」无缝切换；`update_hosts.ps1` 探测地址时也按「有网关 > 有线 > 路由度量小」取优先，hosts 三项始终指向有线 IPv4（拔掉网线才回退到无线/热点）。
- **仅处理「已连接」网卡**：用 `MediaConnectionState -eq 'Connected'` 过滤，避免把已启用但未连上的虚拟/禁用网卡也算进来。
- **VPN / 虚拟网卡自动排除**：枚举时再排除 `Tunnel` 类型以及名称命中正则（VPN、TAP、OpenVPN、WireGuard、Wintun、Cisco/AnyConnect、GlobalProtect、Forti、ZScaler、ZeroTrust、隧道、虚拟、WAN Miniport、SoftEther、Check Point、SonicWall、SSL VPN 等）的网卡，**确保自动触发绝不会误改 VPN 隧道网卡而导致 VPN 断连**。关键字可在 `config.json` 的 `ExcludeAdapters` 自定义或扩展。

## 六、默认网络参数（有线静态直接套用）

| 参数 | 默认值 |
|------|--------|
| IP 地址 | `192.168.1.100` |
| 子网掩码 | `255.255.255.0` |
| 默认网关 | `192.168.1.1` |
| 首选 DNS | `114.114.114.114` |
| 备用 DNS | `101.198.198.198` |
| 接口优先级(metric) | 有线 `10`（优先）/ 无线 `20`（备用） |

### 改参数：编辑 `config.json`（推荐，不用碰脚本）

首次运行脚本会自动在同级目录生成 `config.json`，内容例如：

```json
{
  "DefaultIP": "192.168.1.100",
  "DefaultMask": "255.255.255.0",
  "DefaultGateway": "192.168.1.1",
  "DefaultDNS1": "114.114.114.114",
  "DefaultDNS2": "101.198.198.198",
  "WiredMetric": 10,
  "WirelessMetric": 20,
  "ExcludeAdapters": "VPN|TAP|OpenVPN|WireGuard|Wintun|Cisco|AnyConnect|GlobalProtect|Forti|ZScaler|ZeroTrust|隧道|Tunnel|虚拟|WAN\\s*Miniport|SoftEther|Check\\s*Point|SonicWall|SSL\\s*VPN",
  "UpdateHostsOnAuto": true,
  "UpdateHostsScript": "update_hosts.ps1"
}
```

改完保存即可，**下次运行自动生效**。可用字段：
- `DefaultIP` / `DefaultMask` / `DefaultGateway` / `DefaultDNS1` / `DefaultDNS2`：有线静态的默认参数（选项 1 自动套用，选项 2 作为手动更改的默认值）。
- `WiredMetric` / `WirelessMetric`：接口优先级，数值越小越优先。
- `ExcludeAdapters`：VPN / 虚拟网卡的排除正则（名称命中即跳过，Tunnel 类型内置必排除）。若你的 VPN 网卡名称含特殊字样未被覆盖，把关键字加进这里即可（注意 JSON 里 `\` 要写成 `\\`）。
- `UpdateHostsOnAuto`：自动模式（菜单 1 与「网络变化自动触发」）完成后是否**同步刷新 hosts**，默认 `true`。设为 `false` 可关闭该联动。
- `UpdateHostsScript`：被调用的 hosts 刷新脚本文件名（位于本目录），默认 `update_hosts.ps1`，一般无需改动。
- 若 `config.json` 损坏或删除，脚本会重建为默认并沿用内置值。

## 七、典型使用场景

**场景 A：在家，有线 + 无线同时在线（推荐日常用法）**
1. 插上网线、连上家庭 Wi-Fi；
2. 双击 `.bat` → 选 `1`；
3. 脚本一次性把两卡都配好：有线 `192.168.1.100`/10（优先走），无线 DHCP（备用）。
4. 此后**插线走有线、拔线自动切无线**，无需再跑脚本。

**场景 B：只连 Wi-Fi（拔掉网线）**
1. 连上任意 Wi-Fi；
2. 双击 `.bat` → 选 `1`；
3. 自动识别为无线 → DHCP 动态获取，并做连通性校验。

**场景 C：临时改有线静态的某个值**
- 选 `2`，按提示输入新 IP / 网关 / DNS，不需要改的直接回车用默认；多网卡时先选要操作的那张。

**场景 D：启用「网络变化自动触发」**
1. 双击 `.bat` → 选 `4`，注册计划任务（需管理员，以 SYSTEM 身份运行）；
2. 以后插拔网线、连不同 Wi-Fi、**切手机热点等任何联网方式变更**时，**脚本自动以静默模式修正配置**，规则依旧（有线静态、无线 DHCP，且同时连接时优先走有线）；配置完成后还会**立即自动调用 `update_hosts.ps1` 刷新 hosts**，把 `know.com` / `host.docker.internal` / `gateway.docker.internal` 指向「当前正在使用」的 IPv4（仅改未注释行，注释与其它内容原样保留；hosts 直接覆盖写入，不生成备份文件）；
3. **关键改进（不再打断无线）**：自动触发是「幂等非侵入」的——无线若已是 DHCP（切换 Wi-Fi 后系统本就会自动续租）就**完全不动**，只有检测到「残留静态 IP」才重置一次；有线若已是静态也跳过。因此正常切换网络**不会再导致无线断网**；
4. 运行记录在 `network_switch.log`；想停用 → 选 `5`。

## 八、备份与一键恢复

- **自动备份（仅针对网卡 IP 配置）**：每次执行选项 1/2/3 切换前，脚本会把目标适配器当前的 IP / 掩码 / 网关 / DNS（及是否 DHCP）写入 `backup.json`。
- **一键恢复**：选 `6`，脚本读取 `backup.json`，把适配器还原到切换前的状态（静态按保存值+前缀长度换算掩码；DHCP 则恢复为自动获取）。
- 适用于：切错配置、想回到上一次状态等场景。
- **⚠️ hosts 文件不生成任何备份**：`update_hosts.ps1` 对 `hosts` 是**直接覆盖写入**（不创建 `hosts.*.bak` 之类的备份文件，避免备份堆积占用空间）。若需回退 hosts 改动，只能手动编辑 `C:\Windows\System32\drivers\etc\hosts`（把那三行的 IP 改回目标值）；或先运行 `update_hosts.ps1 -Diag` 查看当前值再决定。`backup.json` 只管网卡 IP，不管 hosts。

## 九、连通性校验

每次切换完成后，脚本会：
- **ping 默认网关**：不可达 → 提示「本地链路可能异常」（检查网线/Wi-Fi 是否真连上）；
- **ping 首选 DNS（114.114.114.114）**：不可达 → 提示「可能无外网，但本地链路正常」（多为没连互联网，非本机配置问题）；
- 无线 DHCP 模式没有静态网关，会提示「未找到默认网关（DHCP 模式正常，无需静态网关）」，属正常。
- 自动触发模式下，校验结果写入 `network_switch.log` 而非弹窗。

## 十、可重复执行 & 健壮性

- 静态 IP 用 `set address static` 是**替换**而非追加，不会叠加；设 DNS 前会先 `delete dns all` 清空，避免备用 DNS 越加越多。
- DHCP 配置完全**幂等**，反复切回无副作用。
- **自动触发为幂等非侵入**：无线若已是 DHCP 则跳过（不打断系统 DHCP 续租），仅修正残留静态；有线若是静态也跳过。避免频繁切换网络时无线被误重置而断网。
- 每次运行都是全新进程，无残留状态；可在有线/无线间无限往返切换。

## 十一、自定义 & 常见问题

- **改网络参数**：编辑 `config.json`（见第六节），别改 `.ps1` 以免弄坏编码。
- **中文乱码？** 不应出现。`.ps1` 是 UTF-8 BOM 编码、`.bat` 纯 ASCII 无中文；本机直接用 `.bat` 启动即可。
- **想让有线用别的固定 IP？** 改 `config.json` 的 `DefaultIP`（选项 1 自动套用），或选 `2` 临时手填。
- **无线为什么不用静态？** 按本脚本规则，无线一律 DHCP，连上即自动获取，最省心；若某张无线确需固定 IP，可手动选 `2`（会按有线逻辑套默认值，必要时自行改成该网段地址）。
- **自动触发会影响 VPN 吗？** 不会。自动触发枚举适配器时已自动排除 Tunnel 类型与常见 VPN 名称（VPN/TAP/OpenVPN/WireGuard/Cisco/AnyConnect/Forti/GlobalProtect/ZScaler/隧道/虚拟 等），VPN 隧道网卡根本不在处理范围内，不会被套静态 IP。若你的 VPN 网卡名称特殊未被覆盖，在 `config.json` 的 `ExcludeAdapters` 加入对应关键字即可。
- **自动触发会刷新 hosts 吗？** 会。启用自动触发（或运行菜单 1）后，网络配置修正完成会自动调用 `update_hosts.ps1`，把 `know.com`、`host.docker.internal`、`gateway.docker.internal` 三项指向当前「优先/正在使用」的 IPv4。**只要当前使用的网络连接方式发生变更（插线/拔线、切 Wi-Fi、切手机热点等），触发事件就会立即刷新 hosts**；且**只改未注释的激活行**，注释与其他配置原样保留；若某主机名在 hosts 中不存在则追加一行。hosts 刷新记录写入 `update_hosts.log`。无线每次 IP 不固定也无需担心——脚本实时探测当前地址，始终与网络环境一致。**hosts 为直接覆盖写入、不生成备份文件**；内容无变化时自动跳过写盘（幂等，不冗余）。若想关闭该联动，把 `config.json` 的 `UpdateHostsOnAuto` 设为 `false`。
- **拔掉网线切到手机热点，自动触发没更新 hosts？** 真正根因（已修）：计划任务以 **SYSTEM 身份**（`/RU SYSTEM`）运行，子进程 `update_hosts.ps1` 同样以 SYSTEM 运行，而它原先的「UAC 自提权」逻辑只判断 `IsInRole(Administrator)`（SYSTEM 下常为 `$false`），于是执行 `Start-Process -Verb RunAs` 请求交互式 UAC 提权——SYSTEM 没有交互桌面、UAC 无法 consent，提权**静默失败、原进程直接退出**，hosts 因此没更新。你手动双击 `.bat` 时是真人管理员（UAC 提权成功）所以正常。新版已修复：两个脚本都改为「**SYSTEM 或已是管理员就直接执行，绝不走 UAC**」。此外顺带修了两处会"看似点火却没更新"的隐患：① IP 探测改为**只认当前已连接的网卡**（不再因 `Get-NetIPConfiguration` 聚合到已断开有线网卡的残留网关而误填旧静态 IP，或拿到空地址 `exit 1`）；② hosts 更新逻辑修正——目标主机名**只出现在注释里**时也会正确追加激活行（旧逻辑会误判"已存在"而跳过）。若你用的是旧版脚本，重选菜单 `4` 重新注册一次，再按上一条做两步自诊即可。（另：注册时已关闭「仅交流电源运行」限制，电池供电也能触发——这是额外健壮性改进，与此问题无关。）
- **自动触发没反应 / hosts 不更新？** 先区分「任务有没有点火」与「hosts 逻辑是否出错」两步自诊：
  1. **任务是否点火**：看 `network_switch.log`（脚本目录里）有没有 `自动模式启动（由网络变化事件触发）` 这一行。
     - **没有这一行** → 任务根本没被网络变化触发。新版已做两处加固：① 注册时**自动启用事件通道** `Microsoft-Windows-NetworkProfile/Operational`（该通道在不少系统默认是禁用的——禁用则永不记录事件、触发器形同虚设）；② 触发事件从仅 `10000`（连接）扩展到 **`10000` 或 `10001`（连接/断开都触发）**，覆盖插拔网线、切 Wi-Fi、切热点所有场景。请重选菜单 `4` 重新注册一次。若仍无此日志，打开 `taskschd.msc` 看 `NetworkConfigAutoSwitch` 的「上次运行时间 / 上次运行结果」确认，或检查事件查看器里该通道是否真的启用。
     - **有这一行，但 hosts 没变** → 任务点火了，问题在 IP 探测或 hosts 写入。看 `update_hosts.log`：若有 `检测到 IPv4：x.x.x.x` 则说明已探测到地址并尝试更新；若只有 `未检测到可用 IPv4` 则是触发瞬间网络还没就绪（新版已改为只认「当前已连接」网卡、且重试 6 次 × 3 秒规避）。把 `network_switch.log` 与 `update_hosts.log` 一起发我即可精确定位。
- **怎么清理「网络变化自动触发」任务？** 两种办法：① 选菜单 `5`（停用自动触发），脚本会按确切任务名 `NetworkConfigAutoSwitch` 卸载，并**额外扫描全部计划任务**，删除任何「动作指向本脚本」的残留脏任务（早期异常注册尝试留下的、名字不同的任务也能清掉）；若本就没有任务则提示「未发现已注册任务」，属正常。② 手动清理：运行 `taskschd.msc` 打开任务计划程序 →「任务计划程序库」，找 `NetworkConfigAutoSwitch` 或动作里包含 `network_config.ps1` 的任务 → 右键删除；或在管理员 PowerShell 中执行：
  ```powershell
  Get-ScheduledTask | Where-Object { $_.TaskName -like '*NetworkConfig*' -or ($_.Actions.Execute -like '*network_config.ps1*') } | Unregister-ScheduledTask -Confirm:$false
  ```
  > 旧版脚本在多次注册失败后，可能遗留了「名字不叫 NetworkConfigAutoSwitch、但动作仍指向本脚本」的脏任务；菜单 5 现已能自动识别并清理这类任务，无需手动逐个找。
- **改完仍有异常？** 把脚本打印的那行诊断（适配器类型 / 当前 IP / 网关）或选 `7` 的诊断输出原样反馈，便于精确定位。
- **hosts 不备份了，误改了怎么办？** 因为 hosts 是直接覆盖、不再生成 `hosts.*.bak`，回退只能手动：编辑 `C:\Windows\System32\drivers\etc\hosts`，把 `know.com` / `host.docker.internal` / `gateway.docker.internal` 三行的 IP 改成你想要的即可（或用 `update_hosts.ps1 -Diag` 先查看当前值）。另外，旧版本曾生成过一批 `hosts.YYYYMMDD_HHmmss.bak` 文件残留在 `etc` 目录，可手动删除它们以释放空间（新版不会再产生这类文件）。
