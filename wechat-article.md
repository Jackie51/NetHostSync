# 换网线 hosts 总不更新？我用一套 PowerShell 脚本把 Windows 网卡切换自动化了

> 一台笔记本，家里有线、无线、手机热点来回切。每次切网络都要手动改 IP、手动改 hosts，烦到想砸电脑。
> 于是我写了 **NetHostSync**——插拔网线/连断 WiFi 时自动切 IP、自动刷新 hosts，全程无感。
> 本文聊聊它怎么做的，以及我踩过的那些坑（比代码本身更有价值）。

---

## 一、痛点：多网络环境下的人肉运维

如果你也符合下面任意一条，那这篇文章对你有用：

- 在家用有线（静态 IP）、出门用无线（DHCP），每次切换都要 `netsh` 敲一遍；
- 开了 Docker，容器里要靠 `host.docker.internal` 访问宿主，结果一切网络宿主 IP 就变了，容器全挂；
- 有几台机器要"指向同一套内部服务域名"，hosts 文件得手动同步，漏一个就 502；
- 想要"插上网线就自动套静态 IP、拔了就回 DHCP"，但 Windows 自带的"以太网未连接时禁用"根本不是这回事。

Windows 本身不提供"按网络切换 IP 配置"的原生能力。**这件事只能自己造轮子。**

---

## 二、NetHostSync 怎么做：三重触发，不漏一次

核心思路不是"轮询到底"，而是**事件优先 + 兜底轮询**，既灵敏又不空转。

```
网络变化（插线/断网/连 WiFi）
        │
        ▼
┌─────────────────────────────────────────────┐
│  触发层（三重，任一命中即执行）              │
│  1. 事件触发器：NetworkProfile 10000(连)    │
│              / 10001(断)  ← 最灵敏          │
│  2. 登录 / 开机触发器                       │
│  3. 每 5 分钟轮询兜底（怕事件漏抓）         │
└─────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────┐
│  执行层（SYSTEM 权限的计划任务）            │
│  · 选最优 IPv4（有线>无线、有网关、度量小） │
│  · 有线→套默认静态 IP；无线→DHCP           │
│  · 刷新 hosts 目标行（幂等，不重复追加）    │
└─────────────────────────────────────────────┘
```

执行体是一个以 **SYSTEM** 身份运行的计划任务，动作是：

```powershell
powershell -File network_config.ps1 -Auto
```

`-Auto` 模式**不进交互菜单**，直接跑自动切换逻辑，跑完即退出——这是关键（后文踩坑会展开）。

hosts 刷新是幂等的：只改目标域名（如 `know.com` / `host.docker.internal` / `gateway.docker.internal`）所在行的**首列 IP**，其余缩进、内联注释原样保留，不会越改越乱。

---

## 三、怎么用：装一次，之后别管

部署只需 4 个文件同目录：

```
network_config.ps1      # 主逻辑 + 菜单
update_hosts.ps1        # hosts 刷新
NetHostSync.psm1        # 纯变换模块（可选，缺失会自动回退内置逻辑）
config.json             # 配置（默认 IP / 目标域名 / 轮询间隔等）
```

安装步骤（管理员）：

1. 把上面 4 个文件放进一个**本地目录**（⚠️ 不能放网络共享，原因见踩坑 4）；
2. 右键 `network_config.ps1` 用**管理员 PowerShell** 运行；
3. 菜单选 `4`（启用自动触发），会注册计划任务 `NetworkConfigAutoSwitch`；
4. 验证：看 `network_switch.log` / `update_hosts.log` 有无更新记录。

日常想手动管也行：菜单 `2` 设静态、`3` 设 DHCP、`1` 交回自动。

**手动设置优先**：如果你手动给某网卡配了 IP，自动任务会**记住并跳过它**（存 `manual_override.json`），不会再被"有线强制静态"覆盖回去。想交回自动，执行一次菜单 `1` 即可清空。

---

## 四、踩过的坑（这一节建议反复读）

这些坑花了我好几轮才定位清楚，每一个都是"看起来能跑、实际静默失败"。

### 坑 1：`-Auto` 从来没被路由到自动逻辑
任务注册成功了，日志却一片死寂——hosts 永远不更新。
排查发现：计划任务动作是 `-Auto`，但脚本里**根本没有把 `-Auto` 路由到 `Invoke-AutoConfig`**，结果落到交互菜单的 `Read-Host` 死等。SYSTEM 下没有真人输入，脚本超时被杀，`Invoke-HostsUpdate` 永远执行不到。
**教训**：计划任务跑脚本，必须有一条明确的"非交互"入口，且要在参数解析后**立刻路由、立即 exit**，绝不能掉进交互循环。

### 坑 2：杀软（360 安全卫士）静默拦截
脚本放某目录，任务"显示已注册、却从不执行"。
根因是 **360 把脚本及其执行内容当威胁拦了**，表现就是"任务跑不起来"的假象。
**解法**：把部署目录加入 360 信任列表。同理，Defender / 火绒若有拦截也要加白。

### 坑 3：SYSTEM 跑脚本，访问不了网络共享
计划任务以 SYSTEM 运行，映射盘 / `\\server\share` 都不可见。
**所以部署文件必须放在本机本地路径**，别想偷懒放 NAS 共享。

### 坑 4：多机部署时模块版本冲突
另一台机器 `update_hosts.ps1` 调用 `NetHostSync.psm1` 时直接报错、hosts 更新中断。
根因是那台的 `NetHostSync.psm1` 版本不兼容、对某些 hosts 行抛异常，而原调用**没有 try/catch**，异常直接把脚本打断。
**解法（已治本）**：把模块调用整段包进 `try/catch`，异常时自动降级到脚本内置的同款逻辑。"有模块 / 无模块 / 模块出错"三种情况都能正常更新 hosts。

### 坑 5：提权死结 + 括号未闭合
早期菜单 4 自提权时参数传错，被全局守卫 `exit` 挡死，任务压根没注册；后来又因插入代码大括号没闭合，导致管理员运行直接闪退。
**教训**：提权子调用要路由到明确的安装分支；改守卫/分支逻辑后务必跑一次语法校验。

---

## 五、开源与结语

项目已开源，欢迎把玩、提 Issue、拍砖：

🔗 **GitHub：https://github.com/Jackie51/NetHostSync**

它现在是我个人工作流里"装完就忘"的那类工具——插拔网线不再想 hosts 的事，这才是自动化的意义。

如果你也常被多网络环境折磨，或者想看更深入的实现细节（比如"选最优 IPv4"的优先级算法），欢迎在评论区聊。也欢迎关注本号，我会持续分享这类"从一个真实痛点做到开源交付"的小工具。

---

> 【配图建议】
> 1. 文首：一张"插拔网线 → 自动切换"的对比图（人肉 vs 自动），架构图已生成：`docs/architecture.png`（见第二节）；
> 2. 文末：GitHub 仓库截图 / 公众号名片二维码。
> 【发布提示】粘进公众号编辑器后，代码块会自动转为等宽样式；标题建议配封面图（1200×675），可用 Canva 快速做。
