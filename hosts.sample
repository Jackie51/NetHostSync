# ============================================================
# hosts.sample —— 本示例展示 NetHostSync 会处理哪些 hosts 行。
#
# 把它对照你真实的 C:\Windows\System32\drivers\etc\hosts 理解：
#   - 工具「只替换未被注释的正向解析行」里的 IPv4；
#   - 已被 # 注释掉的行、以及带内联 # 注释的部分都会被原样保留；
#   - 下面任何一个 HostsTargets 里列出的域名，若在 hosts 中没有
#     未被注释的条目，则会被自动追加（指向当前活动 IPv4）。
# ============================================================

# ---------- 工具会更新/追加的条目（未注释） ----------
# 这些行的 IP 会被自动替换为「当前活动 IPv4」：
192.168.1.55 know.com
192.168.1.55 host.docker.internal
192.168.1.55 gateway.docker.internal

# 也支持「多主机名写在同一行」：
# 192.168.1.55 api.local web.local

# ---------- 工具不会改动的部分 ----------
# 1) 已被注释掉的条目 —— 原样保留，不会解锁：
# 192.168.1.55 know.com

# 2) 带内联注释的条目 —— 注释部分原样保留，只换 IP：
# 10.0.0.5 old-server.local   # 这是旧的，留着备忘

# 3) 其它与 HostsTargets 无关的条目 —— 完全不动：
127.0.0.1       localhost
::1             localhost
